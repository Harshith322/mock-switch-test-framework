[pytest]
markers =
    smoke: quick sanity checks
    functional: functional/regression tests
    performance: traffic and performance-style tests
testpaths = tests
addopts = -v --html=report.html --self-contained-html
